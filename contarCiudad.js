const giftsCities = [12, 3, 11, 5, 7];
const maxGifts = 20;
const maxCities = 3;


// la suma más alta de regalos a repartir
// visitando un máximo de 3 ciudades
// es de 20: [12, 3, 5]

function getMaxGifts(giftsCities, maxGifts, maxCities) {


    if(maxCities >=1 && maxGifts >= 1 && giftsCities.length >= 1){

    };
    
};

